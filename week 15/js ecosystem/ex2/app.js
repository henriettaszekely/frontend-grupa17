import { sum } from './utils.js'
import show from './export-default.js';

console.log(sum([1, 2, 3]));
console.log(user);
show()

// 1. Un modul este un fisier - tine o parte de cod pe care il imparte cu alte fisiere
